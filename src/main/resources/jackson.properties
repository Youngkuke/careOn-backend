# API 명세에 따라 모든 요청/응답 JSON 필드명을 snake_case로 통일한다.
# (예: carerId -> carer_id, accessToken -> access_token, isExpired -> is_expired)
# Jackson 라이브러리 버전에 의존하지 않도록 Java 설정 대신 프로퍼티로 지정한다.
spring.jackson.property-naming-strategy=SNAKE_CASE
